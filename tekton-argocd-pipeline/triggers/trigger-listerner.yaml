apiVersion: tekton.dev/v1alpha1
kind: EventListener
metadata:
  name: cicd
spec:
  serviceAccountName: pipeline
  triggers:
    - name: cicd-trig
      interceptors:
        - cel:
            filter: "header.canonical(body.pull_request.head.branch == 'master')"
      binding:
        name: binding-cicd
      template:
        name: template-cicd
